import React from 'react';
import PropTypes from 'prop-types';

const  <%= name %> = props => {
    return (
        <div>

        </div>
    );
};

<%= name %>.propTypes = {

};

export default <%= name %>;
